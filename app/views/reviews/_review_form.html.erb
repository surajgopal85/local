<div class="container">
  <%= simple_form_for ([@experience, @review]) do |f| %>
    <%= f.input :content %>
    <%= f.input :rating, collection: 1..5 %>
    <%= f.submit "Add Your Review"  %>
  <% end %>
</div>
