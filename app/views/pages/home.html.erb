<% render 'shared/navbar' %>

<div class="home-background">
<%= image_tag "new-logo-local-small.jpg", style: "width: 80px; height: 80px" %>
<%# <%= cl_image_tag("local/new-logo-local_Small_ck67wa",
      width: 100, height: 100, crop: :fill) %>
  <h4>To Find Culture, Go Local</h4>
  <% if user_signed_in? %>
    <%= link_to "My Profile", profile_path, class: "btn btn-ghost" %>
  <% else %>
    <%= link_to "Get Started", survey_path, class: "btn btn-ghost" %>
  <% end %>
</div>

<%# need a button, design in figma first %>
<%# how do I center the text and everything (logo, button) in the middle vertically? %>
<%# need a version of the v2 logo with no whitespace %>
