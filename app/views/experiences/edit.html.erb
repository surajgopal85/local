<div class="container">
  <%= simple_form_for(@experience) do |f| %>
    <%= f.input :name %>
    <%= f.input :location %>
    <%= f.input :description %>
    <%= f.input :image_url %>
    <%= f.input :address %>
    <hr>
    <%= f.simple_fields_for :experience_tags do |experience_tag| %>
      <%= render 'experience_tag_fields', f: experience_tag %>
    <% end %>
    <div class="links">
      <button class="button"><%= link_to_add_association 'Add Tag', f, :experience_tags %></button>
    </div>
    <div class="d-flex justify-content-center my-3">
      <%= f.submit "Update Experience!" %>
    </div>
  <% end %>
</div>
